query Search($query: String!) {
  search(query: $query) {
    id
    name
    categoryName
    city
    state
    street
    postalCode
    temporarilyClosed
    permanentlyClosed
    imageUrls
    location
    reviewsCount
    reviewsDistribution
  }
}
