query GetSkateSpots($cursor: Int, $limit: Int) {
  getSkateSpots(cursor: $cursor, limit: $limit) {
    id
    name
    categoryName
    street
    city
    postalCode
    state
    temporarilyClosed
    permanentlyClosed
    location
    reviewsCount
    reviewsDistribution
    imageUrls
  }
}
