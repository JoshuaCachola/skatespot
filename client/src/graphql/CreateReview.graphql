mutation CreateReview($review: String!, $userId: Int!, $skateSpotId: Int!, $rating: Int!, $imgFiles: [Upload!]) {
  createReview(review: $review, userId: $userId, skateSpotId: $skateSpotId, rating: $rating, imgFiles: $imgFiles)
}
