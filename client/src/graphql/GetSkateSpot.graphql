query GetSkateSpot($name: String!) {
  getSkateSpot(name: $name) {
    id
    name
    categoryName
    city
    state
    street
    postalCode
    temporarilyClosed
    permanentlyClosed
    imageUrls
    location
    reviewsCount
    reviewsDistribution
  }
}
