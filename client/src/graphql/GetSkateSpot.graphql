query GetSkateSpot($id: Int!) {
  getSkateSpot(id: $id) {
    id
    name
    categoryName
    city
    state
    street
    postalCode
    website
    temporarilyClosed
    permanentlyClosed
    imageUrls
    location
    reviewsCount
    reviewsDistribution
  }
}
