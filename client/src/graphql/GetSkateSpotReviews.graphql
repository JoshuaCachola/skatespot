query GetSkateSpotReviews($skateSpotId: Int!) {
  getSkateSpotReviews(skateSpotId: $skateSpotId) {
    id
    review
    rating
    imageUrls
    createdAt
    user {
      id
      username
      firstName
      lastName
    }
  }
}
